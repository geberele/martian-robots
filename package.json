{
  "name": "martian-robots",
  "version": "1.0.0",
  "description": "The Martian Robots Problem",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify js/src/App.js -o js/dist/martian-robots.js --debug -t [ babelify --presets [ env react ] ]"
  },
  "author": "GM",
  "license": "ISC",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "browserify": "^16.1.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "babelify": "^8.0.0"
  }
}
